// ============================================================================
// PTEOffsetCodec — Compile unit (currently header-only; kept for future
// out-of-line helpers or explicit template instantiations)
// ============================================================================
#include "pte_offset_codec.h"

namespace ParametricDramDirectoryMSI
{
    // All methods are inline in the header.
    // This .cc exists so the build system picks up the translation unit
    // and any future non-inline additions live here.
}
