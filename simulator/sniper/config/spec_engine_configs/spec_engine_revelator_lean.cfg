# Lean Revelator spec-engine selector — sets only the engine type.
# Avoids touching [perf_model/mimicos_host] so it won't override the
# 4KB-only allocator settings further up the include chain.

[perf_model/mmu/spec]
type = "revelator"
