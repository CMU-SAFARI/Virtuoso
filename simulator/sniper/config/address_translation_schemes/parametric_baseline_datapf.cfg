# Parametric — Baseline translation, NO TLB prefetch  (data-PF held constant)
# ============================================================================
# Matrix point 1/3 for the TLB-prefetch study. Identical to parametric_baseline.cfg
# but with the shared data-side prefetch layer (L1D IP-Stride data-only + L2 Streamer)
# included AFTER the core config so it overrides meteor_lake_pcore's prefetch settings.
# No TLB prefetching (mmu_base.cfg has prefetch_enabled = false).

#include ./common_configs/base_system_sniperspace.cfg
#include ./core_configs/meteor_lake_pcore.cfg
#include ./prefetcher_configs/datapf_ipstride_notrans_streamer.cfg
#include ./mmu_configs/mmu_base.cfg
#include ./mmu_configs/l2tlb_chirp.cfg
#include ./pagetable_configs/4_level_radix.cfg
#include ./pagetable_configs/range_table.cfg
#include ./dram_configs/readwrite_1ctrl_153gbps.cfg
#include ./physical_memory_allocators/reserve_thp.cfg
