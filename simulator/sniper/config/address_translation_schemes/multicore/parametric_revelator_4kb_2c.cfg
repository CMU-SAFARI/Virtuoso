# Parametric — Revelator 4KB-Only (2-core)
# ============================================
# Same as parametric_revelator_4kb.cfg but configured for 2 cores.
# NUCA is the shared LLC (cache_size in meteor_lake_pcore.cfg = 8192 total).

#include address_translation_schemes/parametric_revelator_4kb.cfg

[general]
total_cores = 2
