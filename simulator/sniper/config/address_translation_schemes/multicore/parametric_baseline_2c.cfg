# Parametric — Baseline Translation (2-core)
# =============================================
# Same as parametric_baseline.cfg but configured for 2 cores.
# NUCA is the shared LLC (cache_size in meteor_lake_pcore.cfg = 8192 total).

#include address_translation_schemes/parametric_baseline.cfg

[general]
total_cores = 2
