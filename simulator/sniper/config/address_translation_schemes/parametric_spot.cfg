# Parametric — SpOT (Speculative Offset-based Translation)
# =========================================================
# Same base system, core, DRAM, and page-table configs as parametric_baseline
# but with SpOT allocator, speculative MMU, and SpOT engine.

# Base system (simulation basics)
#include ./common_configs/base_system_sniperspace.cfg

# Core and cache configuration (Meteor Lake P-core)
#include ./core_configs/meteor_lake_pcore.cfg

# MMU configuration (speculative — needed for SpOT engine)
#include ./mmu_configs/mmu_spec.cfg

# Page table configuration
#include ./pagetable_configs/4_level_radix.cfg
#include ./pagetable_configs/range_table.cfg

# DRAM configuration — same as parametric baseline
#include ./dram_configs/readwrite_1ctrl_153gbps.cfg

# Physical memory allocator (SpOT)
#include ./physical_memory_allocators/spot.cfg

# Speculative engine (SpOT)
#include ./spec_engine_configs/spec_engine_spot.cfg
